setCurve()
==========

The hec-python-library equivalent of Jython method **setCurve()**:

.. include:: _not_implemented.rst

This library allows the curve label or number to be specified on each call to ``rate(...)`` or ``reverse_rate(...)``
